{
  "name": "silnik",
  "version": "0.0.1",
  "description": "To debug the code in vscodium or vscode, install",
  "main": "src/natexec.js",
  "scripts": {
    "create-node": "ln -s `which node` node",
    "dev": "node-gyp build --debug && npm run create-node",
    "build": "npm i & npm run create-node",
    "test": "node ./tests/test.js"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VioletFlare/silnik.git"
  },
  "gypfile": true,
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.5.0"
  }
}
